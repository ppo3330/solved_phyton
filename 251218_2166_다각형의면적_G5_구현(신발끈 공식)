#O(N)
#처음에는 삼각형으로 나누어서 하려했는데 오목 다각형인경우 불가능, 신발끈공식이라는 다각형 면적 구하는 공식이 있음.
N = int(input())
triangles = [tuple(map(int,input().split())) for _ in range(N)]

result = 0
for i in range(N):
    x1,y1 = triangles[i]
    x2,y2 = triangles[(i+1)%N]
    result += x1*y2 - y1*x2
    
print(round(abs(result)/2,2))
